{
  "name": "tf2-join-alert",
  "version": "0.0.1",
  "description": "Steam bot that sends a message to a user when a tf2 server is joined",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./build",
    "build": "tsc",
    "start": "node ./build/index.js",
    "build:dev": "npm run clean && npm run build",
    "build:start": "npm run clean && npm run build && npm run start",
    "format": "prettier --write ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/testsnake/TF2-Join-Alert.git"
  },
  "author": "testsnake",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/testsnake/TF2-Join-Alert/issues"
  },
  "homepage": "https://github.com/testsnake/TF2-Join-Alert#readme",
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ping": "^0.4.4",
    "pino": "^8.19.0",
    "pino-pretty": "^10.3.1",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.2"
  },
  "packageManager": "pnpm@8.15.1"
}
